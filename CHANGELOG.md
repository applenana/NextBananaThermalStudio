# Changelog

本项目所有显著变更记录于此。版本号遵循 [SemVer](https://semver.org/lang/zh-CN/)，发布日志按时间倒序。

## 未发布

### 新增 (Added)

- 设置页新增跨端热成像线性校准向导：中央 ROI 连续多帧取样、两点/多点最小二乘拟合、稳定性与误差提示，并可原子写入或恢复设备校准系数；Android 不新增底部导航项。
- 开启热成像画面时通过 `thermal view` 查询设备的热像 X/Y 偏移与缩放，并按设备端相同的源窗口、Q10 坐标和边缘钳制规则渲染。
- 拍摄和录制元数据保存已同步的热像视图参数，软件图库回放可复现采集时画面。
- 设备图库支持 HTPH v3 照片尾部的热像缩放与 X/Y 偏移，混合图下载、缓存、预览和导出会自动复现拍摄时的热像视图，同时继续兼容 v1/v2 照片。
- Windows 连接设备后自动开启热成像投屏，并补齐多点测温、清理标记、全屏视图及悬浮控制按钮。
- Windows 全屏改为仅切换软件内的热像视图，不再改变系统窗口最大化状态；单点测温支持单击固定、再次单击移动。
- Windows / Android 共用持续温度记录器，每秒保存最高、最低、平均、固定单点、所有多点及多点平均温度，普通视图与全屏视图共享同一会话。
- 温度趋势新增测温点曲线：单点模式显示固定点温度，多点模式显示所有当前测温点的平均温度。
- 温度趋势区域新增跨端导出弹窗，可多选导出 CSV 原始数据、PNG 趋势图和 JSON 结构化数据；移动端 PNG 直接保存到系统相册。
- 普通与全屏温度趋势标题栏新增“保存当前会话并新建”按钮，确认后先归档当前数据，再从下一帧开始新记录。
- 温度趋势标题栏新增记录频率设置，可选 5 次/秒、2 次/秒、1 次/秒、每 2/5/10 秒一次，并在 Windows / Android 持久保存。
- 普通与全屏温度趋势新增跨端暂停/恢复记录按钮；暂停只停止温度采样，热像画面继续显示，恢复后立即续写原历史会话。
- 软件图库的视频回放新增热点、冷点追踪复选框；勾选后预览与导出均以快速过渡逐帧追踪极值，MP4 或逐帧 PNG 同步写入对应温度标签。
- 新增跨端“温度历史”栏目，以版本化 `metadata.json + samples.jsonl` 在本地持久保存测温会话；支持名称/设备搜索、今天/近 7 天/近 30 天筛选。
- 历史详情支持最高、最低、平均、固定单点、多点均值及各独立测温点曲线开关，提供时间范围切换、触摸读数、统计摘要和最近采样表格。
- 历史会话支持重命名、结束归档、永久删除、导入既有 JSON 记录，以及复用 CSV / PNG / JSON 多格式导出。
- 首次采样自动创建历史会话；设备更换、断流超过 30 秒、持续 24 小时或达到 10 万条记录时自动安全分段。
- 实时热像主画面新增低干扰透明悬浮温度色标，可直接关闭或在画面内拖动，并可切换横向/超窄纵向及左/右默认停靠；色条与当前帧实际温区、映射曲线和调色板保持同源。

### 优化 (Changed)

- 温度图例新增 0/25/50/75/100% 透明度调节，100% 时只保留温度彩条；横向图例压缩为最低温、彩条、最高温单行布局，显著减少画面遮挡。
- 热成像投屏升级为 `thermal view` v2 协商：上位机按传感器、屏幕模式和声明尺寸解析热流，并在 MLX 方屏下复原设备端缩放/偏移链路；旧固件无响应时仍按 32×24、768 个小端浮点数兼容解析。
- 最高温和最低温追踪标记改为快速 ease-out 过渡，目标位置变化时不再瞬移。
- 修复 Windows 窗口从最大化恢复后四周出现黑边：Flutter 内容现在覆盖完整客户区，同时保留边缘缩放命中区。
- 优化温度历史栏目的得意黑排版层级，参考实时与图库栏目降低标题、会话名称、统计值与状态标签的字号和字重。
- 温度历史支持对当前筛选结果批量全选、逐条勾选、清空选择与批量删除，并同步清理本地原始采样目录。

## [v0.2.3] - 2026-05-15

### 修复 (Fixed)
- **串口首次连接收不到数据**：`SerialService.open()` 与后台探测 `_probeDeviceCore()` 打开串口后未显式拉高 DTR / RTS，导致 RP2040 + TinyUSB CDC 固件因 `cdc_connected = false` 丢弃所有 TX。表现为必须先用浏览器 Web Serial「预热」一次才能正常收包。现强制 `dtr = on`、`rts = on`，开箱即用。

### 优化 (Changed)
- **默认窗口尺寸由 935×755 加宽至 1280×820**：原尺寸下实时 Tab 右侧列在多处出现 `RenderFlex overflowed` 警告，新默认值给列表 / 按钮 / 控制台留足空间。已通过 `SharedPreferences` 持久化旧尺寸的用户不受影响（可在设置里恢复默认）。
- `windows/runner/main.cpp` 注释统一改为英文，避免 MSVC 在 CP936 代码页下将 C4819 提升为 error 阻断构建。

## [v0.2.2] - 历史版本

- fix(serial): 改用 `Isolate.spawn` 顶层 entry 让探测进度消息正常回主线程。

## [v0.2.1] - 历史版本

- perf(serial): 串口探测放进后台 isolate 避免卡 UI。

## [v0.2.0] - 历史版本

- feat(ui): 窄屏模式底部导航栏适配。
- docs: 截图改为并排表格布局；README 加入实时与图库截图。

## [v0.1.1] - 历史版本

- feat: 启动闪屏 / 自动连接 / 掉线重连 / 连接栏布局重构。

## [v0.1.0] - 历史版本

- feat: 从 Python/Tk 上位机迁移到 Flutter，并接入 CI workflow 与 README。
